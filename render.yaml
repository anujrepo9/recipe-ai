services:
  - type: web
    name: recipe-ai-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python local_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0