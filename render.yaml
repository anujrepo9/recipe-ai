services:
  - type: web
    name: recipe-ai-api
    runtime: python
    buildCommand: apt-get install -y libgomp1 && pip install -r requirements.txt
    startCommand: python local_server.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0